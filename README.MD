# KARAOKE_api

Projeto Django + PostgreSQL

Contem base de dados de titulos de musicas, nome do artista, numero da musica no KARAOKE
para ser consumida por qualquer aplicação que necessite desta base de dados.

## Como rodar
```bash
pip install -r requirements.txt
python manage.py migrate
python manage.py runserver
